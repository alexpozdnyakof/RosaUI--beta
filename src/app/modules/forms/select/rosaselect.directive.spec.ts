import { RosaselectDirective } from './rosaselect.directive';

describe('RosaselectDirective', () => {
  it('should create an instance', () => {
    const directive = new RosaselectDirective();
    expect(directive).toBeTruthy();
  });
});
