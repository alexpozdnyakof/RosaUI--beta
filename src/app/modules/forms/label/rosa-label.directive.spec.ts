import { RosaLabelDirective } from './rosa-label.directive';

describe('RosaLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new RosaLabelDirective();
    expect(directive).toBeTruthy();
  });
});
