div(class="toast message" '[@fadeAnimation]'="{value: animationState, params:{fadeIn: toastConfig.animation.fadeIn, fadeOut: toastConfig.animation.fadeOut }}" '(@fadeAnimation.done)'="onFadeFinished($event)")
  // ng-container {{ iconType }}
  span {{ data.text }}
  // div('(click)'="close()") close
